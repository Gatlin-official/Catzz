function generateReport(data) {
    // Basic example report generation
    const report = {
        createdAt: new Date(),
        summary: `Report generated with ${data.length} records.`,
        details: data,
    };
    return report;
}
